╔══════════════════════════════════════════════════════════════════════╗
║                    🤖 BOXBI MESSENGER - QUICK START                 ║
║                    Universal HTTP Access Enabled                     ║
╚══════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────┐
│ 🚀 START THE SERVER (Required First!)                               │
└─────────────────────────────────────────────────────────────────────┘

1. Open PowerShell/Terminal
2. Navigate to server directory:
   cd c:\Users\ragha\Downloads\fullstack-chat-main\server-spring

3. Run the server:
   $env:JAVA_HOME = "C:\Program Files\Java\jdk-21"
   .\mvnw.cmd spring-boot:run

4. Wait for: "Started ServerSpringApplication in X seconds"
   ✅ Server is now running on http://localhost:8080

┌─────────────────────────────────────────────────────────────────────┐
│ 💬 OPEN BOXBI MESSENGER                                             │
└─────────────────────────────────────────────────────────────────────┘

METHOD 1: Double-click the file
   → Open: c:\Users\ragha\Downloads\fullstack-chat-main\chat-app.html

METHOD 2: Browser URL bar
   → Type: file:///c:/Users/ragha/Downloads/fullstack-chat-main/chat-app.html

METHOD 3: Share on local network
   1. Find your IP: ipconfig (look for IPv4 Address)
   2. Edit chat-app.html line 340:
      const API_URL = 'http://YOUR_IP:8080';
   3. Share the HTML file with others

┌─────────────────────────────────────────────────────────────────────┐
│ 📝 USING BOXBI MESSENGER                                             │
└─────────────────────────────────────────────────────────────────────┘

NEW USERS:
  1. Click "Sign up" link
  2. Fill in all fields (username, email, first name, last name, password)
  3. Click "Sign Up"
  4. You're automatically logged in!

EXISTING USERS:
  1. Enter username and password
  2. Click "Login"

IN THE CHAT:
  • Type messages in the bottom input box
  • Press Enter or click "Send"
  • Your messages appear on the right (gradient background)
  • Others' messages appear on the left (white background)
  • See when users join/leave
  • Click "Logout" to exit

┌─────────────────────────────────────────────────────────────────────┐
│ ✅ VERIFICATION CHECKLIST                                            │
└─────────────────────────────────────────────────────────────────────┘

✅ Java 21 installed
✅ Backend server running on port 8080
✅ WebSocket connections allowed from all origins (*)
✅ REST API accessible from all origins (*)
✅ H2 database initialized
✅ Boxbi Messenger chat tested and working

┌─────────────────────────────────────────────────────────────────────┐
│ 🔧 TROUBLESHOOTING                                                   │
└─────────────────────────────────────────────────────────────────────┘

❌ "Connection error. Make sure the server is running."
   → Check if server is running on port 8080
   → Restart the server
   → Check firewall settings

❌ "WebSocket connection error"
   → Verify server started successfully
   → Check browser console (F12) for details
   → Ensure API_URL in chat-app.html is correct

❌ Port 8080 already in use
   → Stop other applications using port 8080
   → Or change port in application.properties

❌ Database issues
   → Delete chat_db.mv.db and chat_db.trace.db files
   → Restart server (will recreate database)

┌─────────────────────────────────────────────────────────────────────┐
│ 📊 TECHNICAL INFO                                                    │
└─────────────────────────────────────────────────────────────────────┘

Backend:     Spring Boot 3.2.1 (Java 21)
Database:    H2 (embedded, file-based)
WebSocket:   STOMP over SockJS
Frontend:    Vanilla HTML/CSS/JavaScript

Endpoints:
  • POST /login          - User authentication
  • POST /signup         - User registration
  • GET /users/search    - Search users
  • WS /ws              - WebSocket connection

Database Console:
  • URL: http://localhost:8080/h2-console
  • JDBC: jdbc:h2:file:./chat_db
  • User: sa
  • Pass: (empty)

┌─────────────────────────────────────────────────────────────────────┐
│ 🌐 SHARING BOXBI MESSENGER WITH OTHERS                               │
└─────────────────────────────────────────────────────────────────────┘

SAME COMPUTER:
  → Just open chat-app.html in any browser

LOCAL NETWORK:
  1. Get your IP address (ipconfig)
  2. Update API_URL in chat-app.html to http://YOUR_IP:8080
  3. Share the HTML file
  4. Others open it in their browser

INTERNET (Production):
  → Deploy backend to cloud (Render, Railway, etc.)
  → Update API_URL to your server URL
  → Host HTML on web server (Netlify, GitHub Pages)
  → Add HTTPS and proper security

┌─────────────────────────────────────────────────────────────────────┐
│ ⚠️  SECURITY NOTICE                                                  │
└─────────────────────────────────────────────────────────────────────┘

Current setup allows ALL origins (*) - suitable for:
  ✓ Development and testing
  ✓ Local network usage
  ✓ Trusted environments

For production, implement:
  • Restrict CORS to specific domains
  • Use HTTPS
  • Add JWT authentication
  • Implement rate limiting
  • Add input validation
  • Use secure session management

═══════════════════════════════════════════════════════════════════════

🤖 Welcome to Boxbi Messenger - Your AI-powered chat companion!

For detailed setup instructions, see: SETUP_GUIDE.md
To deploy globally, see: GLOBAL_DEPLOYMENT_GUIDE.md
